#!/bin/sh
for file in $@
do
  if [ -s $file ]; then
    echo format $file ...
    ~/bin/m_exec uncrustify -c ~/bin/std.cfg --no-backup $file
    chmod 644 $file
  else
    echo can not format $file
  fi
done
