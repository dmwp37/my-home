#!/bin/bash
for file in $@
do
  if [ -s $file ]; then
    echo format $file ...
    uncrustify -c `cygpath -wp ~/bin/std.cfg` --no-backup `cygpath -wp $file`
    chmod 644 $file
#    chown dmwp37 $file
#    dos2unix -q $file
  else
    echo can not format $file
  fi
done

